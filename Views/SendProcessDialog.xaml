<Window x:Class="pi_client.Views.SendProcessDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Create Send Process" Height="250" Width="300">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Channel:" Grid.Row="0" Margin="0,5"/>
        <TextBox x:Name="ChannelTextBox" Grid.Row="1" Text="default" Margin="0,5"/>

        <TextBlock Text="Message:" Grid.Row="2" Margin="0,10,0,5"/>
        <TextBox x:Name="MessageTextBox" Grid.Row="3" Text="Hello" Margin="0,5"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Width="80" Margin="5" Click="Cancel_Click"/>
            <Button Content="Create" Width="80" Margin="5" Click="Create_Click"/>
        </StackPanel>
    </Grid>
</Window>